# Pytwitter twitter clone

in order to run the app copy .env.example to .env and change the attributes according to your system.

the run the following command:

```shell
docker compose up --build -d
```